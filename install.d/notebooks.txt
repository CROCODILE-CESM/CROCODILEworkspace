# CrocoGallery notebook IDs rendered into $BASK_PATH/workspace/ when
# --notebooks is passed to install.sh (and included by --all and --workshop).
# One entry per line: <notebook-id> [output-name]. The output name is
# optional (".ipynb" is added if missing) and defaults to the notebook ID.
# It may include folders under workspace/, which are created as needed
# (e.g. "crocodash/tutorial"); a trailing "/" keeps the ID as the filename
# (e.g. "dart/" -> workspace/dart/<notebook-id>.ipynb).
# Blank lines and lines starting with '#' are ignored.
# List available IDs with: crocogallery template --list-notebooks
crocodash.tutorial
crocodash.projects.ocean_only projects/ocean_only
crocodash.projects.cice projects/cice
crocodash.projects.ww3 projects/ww3
crocodash.projects.marbl projects/marbl
crocodash.projects.KitchenSink projects/kitchen_sink
model2obs.tutorial_CrocoLake_map_temperature
dart.tutorial1_real_observations
dart.tutorial2_synthetic_observations
dart.tutorial3_cycling_dart_cesm
